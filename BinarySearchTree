class BST {
    
      boolean search(Node root, int x) {
      if(root==null){
       return false;
      }
      else if(root.data==x){
       return true;
      }else{
          return x<root.data ? search(root.left,x) : search(root.right,x);
      }
      
      
    }
   
}
